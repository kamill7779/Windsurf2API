{
  "name": "windsurf2api",
  "version": "2.0.0",
  "description": "Windsurf-to-OpenAI API proxy with Dashboard + Multi-Channel + API Keys",
  "type": "module",
  "scripts": {
    "build": "tsc && node scripts/copy-dashboard.mjs",
    "dev": "tsc --watch &",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20"
  }
}
